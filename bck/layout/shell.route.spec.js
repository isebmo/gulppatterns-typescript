/* jshint -W117, -W030 */
describe('shell', function() {
    describe('state', function() {
//todo
        /*beforeEach(function() {
            module('app.historique', bard.fakeToastr);
            module('app.core');
            bard.inject(this, '$state', '$rootScope');
        });

        it('should map state historique to url #/historique ', function() {
            expect($state.href('historique')).to.equal('#/historique');
        });

        it('should work with $state.go', function() {
            $state.go('historique');
            $rootScope.$apply();
            expect($state.current.name).to.equal('historique');
        });*/

    });
});
